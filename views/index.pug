extends layout

block content
  h1= title
  p Welcome to #{title}
  //- p #{chatLog}
  ul
    each entry in chatLog
      li 
        p #{entry.text} 
        p #{entry.user} 
        p #{entry.added} 
